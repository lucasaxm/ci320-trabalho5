<%= form_for(@bag) do |f| %>
  <% if @bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bag.errors.count, "error") %> prohibited this bag from being saved:</h2>

      <ul>
      <% @bag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pokeballs %><br>
    <%= f.number_field :pokeballs %>
  </div>
  <div class="field">
    <%= f.label :potions %><br>
    <%= f.number_field :potions %>
  </div>
  <div class="field">
    <%= f.label :trainer_id %><br>
    <%= f.number_field :trainer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
